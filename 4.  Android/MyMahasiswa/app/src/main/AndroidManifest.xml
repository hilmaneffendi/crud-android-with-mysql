<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coder01.mogumogu.mymahasiswa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TampilMahasiswa"
            android:label="List Data Mahasiswa"
            android:parentActivityName=".TampilMahasiswa"></activity>
        <activity
            android:name=".TampilDetailMahasiswa"
            android:label="Detail Mahasiswa"
            android:parentActivityName=".TampilDetailMahasiswa"></activity>
        <activity
            android:name=".InputMahasiswa"
            android:label="Input Mahasiswa"
            android:parentActivityName=".InputMahasiswa"></activity>
    </application>

</manifest>